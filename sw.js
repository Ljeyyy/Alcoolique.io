// Ceci est un Service Worker très basique
// Son unique but est de permettre l'installation de l'application (PWA)

self.addEventListener('fetch', function(event) {
    // On ne fait rien de spécial, on laisse passer les requêtes normalement
});